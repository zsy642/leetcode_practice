文件名数字开头无法直接使用 import 语句
nums=[]*10和[None]*10情况不一样,前者不能nums[3]=10
子数组是数组中连续的非空元素序列,子序列是数组中的非空元素序列
滑动窗口时间复杂度是O(n)
    不要以为for里放一个while就以为是O(n^2)啊,主要是看每一个元素被操作的次数，
    每个元素在滑动窗后进来操作一次，出去操作一次，每个元素都是被操作两次，所以时间复杂度是 2 × n 也就是O(n)。
无限大的写法是:float('inf')