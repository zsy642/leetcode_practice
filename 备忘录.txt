文件名数字开头无法直接使用 import 语句
nums=[]*10和[None]*10情况不一样,前者不能只是[],而且不能nums[3]=10
nums=[[]]*10是十个一模一样的[]引用改一个全会变,正确应该是[[0] for _ in range(10)]
子数组是数组中连续的非空元素序列,子序列是数组中的非空元素序列
滑动窗口时间复杂度是O(n)
    不要以为for里放一个while就以为是O(n^2)啊,主要是看每一个元素被操作的次数，
    每个元素在滑动窗后进来操作一次，出去操作一次，每个元素都是被操作两次，所以时间复杂度是 2 × n 也就是O(n)。
无限大的写法是:float('inf')
input读回来的都是字符串
ord() 像是“查字典”：你给它一个汉字，它告诉你这个字在字典的第几页。
    ord('1') → 49 （字符 '1' 在编码表里的页码是 49）
int() 像是“去引号”：你给它一个看起来像数字的标签，它把它变成能算账的钱。
    int('1') → 1 （真正可以用来加减的数字 1）
字母索引index=ord(i)-ord('a')
哈希表专题的三驾马车
 :在《代码随想录》中，卡哥强调过，根据题目需求，我们会选择不同的哈希结构：
    数组：题目给出的范围很小（比如只有 26 个字母）。
    set (集合)：数值很大、很分散，或者只需要知道“在不在”，不需要统计次数。
    dict (字典/映射)：不仅要知道“在不在”，还要知道这个值对应的下标或者出现的次数。
for x in sth在数组查找时间复杂度为n,在集合/字典key查找为1,但是数组知道索引时,时间复杂度是O(1)
hashmap.get(n1+n2, 0) + 1,可以实现字典key,返回0
如果你希望在键不存在时自动插入一个默认值到字典里，你应该使用 dict.setdefault(key, default) 方法
数组易错点:
    长度是len(nums),但是最后一个索引是len(nums)-1
    分清索引index和数组的内容和nums[index]
只能写2*k不能写2k
类里面的变量是self.x不是x
6//5=1,6/5=1.2
// (地板除)：向下取整（Floor）。int()：向 0 取整（截断小数位，不论正负）。
math.floor()：向下取整（与 // 效果相同）。math.ceil()：向上取整（向正无穷方向）。
remove删除的值,pop是索引
三元表达式:真值表达式 if 条件 else 假值表达式,例如lastmost = root.left if root.left else root.right
二叉树里大多数人这么写lastmost = root.left or root.right,or的规则不同于c,如果前真返回前否则返回后
deque初始化要么就deque(),要么就是可迭代对象比如deque([root])
items() 是字典的方法，返回一个视图对象，包含所有键值对，用于遍历字典。相当于数组的enumerate
heapq.heappush(pri_que,(freq,key))推入的是 (freq,key)元组:堆会优先按freq从小到大排序(因为它是第一个元素).freq 相同,则按key从小到大排序。
只有完美二叉树才是给定一个序列的树是唯一的
每次都有一个头需要特殊处理,每次都需要抓开头的话,试试虚拟节点(对付链表和残缺二叉树的好帮手)
两个状态需要同步联动，容易错位？ 工具箱：Tuple（元组）包装成对入栈。
解决递归的初始化问题:
    1.递归内写nonlocal res,然后外部写res=0
    2.尾递归,直接在返回值那里递归
sort是升序的1234,字典也可以排序和list(一般这俩默认是按key升序)
字典是没有 .sort() 方法的（因为 .sort() 是列表特有的类方法）,sort是原地的,sorted是非原地,且返回一个纯 Key 的列表，而不是一个排序后的字典。
    字典排序正确办法是sorted_by_key = dict(sorted(my_dict.items(), key=lambda x: x[0]))
类变量是在类里面的全局变量，默认在类里面全局可读，可修改。(也就是self.后面的变量)
    不建议把函数内部的临时递归状态挂到 self上。如果这个Solution实例在多线程环境下被复用,或者连续调用两次getMinimumDifference
    self.lastnum可能会留有上一次调用的残留数据，从而引发隐藏的 Bug。可以统一使用 nonlocal，让函数内部的状态保持绝对的独立与封装
递归的情况下如果是x=10,父空间会覆盖子空间修改反之亦然,nonlocal prev, result , g_num, num可以避免这种情况(可以一次性引入)
如果列表 [x, y] 中的 x 发生改变，原列表是否会变取决于 x 本身是不可变对象（如数字、字符串、元组）还是可变对象（如列表、字典）。
    如果 x 是数字或字符串等不可变对象，重新赋值不会改变原列表；如果 x 是可变对象并被原地修改，原列表就会变。